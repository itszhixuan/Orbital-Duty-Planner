import { push, set, ref } from "firebase/database";
import { database } from "../Firebase_config";
/*
export function addEventToDatabase(profileUID, event) {

    const newEventKey = 

}

export function deleteEventInDatabase(event) {
    const name = event.eventName;
    const n
}

export function returnProfileEvents(user) {

}*/